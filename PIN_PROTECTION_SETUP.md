# PIN Защита приложения "Мира"

## Что реализовано

✅ **PIN защита при запуске**
- Экран ввода 4-значного PIN-кода
- Красивая цифровая клавиатура
- Анимация тряски при неправильном вводе
- Индикатор введенных цифр

✅ **Биометрическая аутентификация**
- Face ID / Touch ID поддержка
- Автоматический запуск биометрии при входе
- Кнопка повторной аутентификации
- Включение/выключение в настройках

✅ **Система ограничения попыток**
- 3 попытки ввода PIN
- Блокировка на 30 секунд после 3 неудачных попыток
- Обратный отсчет времени блокировки

✅ **Безопасное хранение**
- PIN хранится в `FlutterSecureStorage` (зашифрованное хранилище)
- Не хранится в SharedPreferences
- Настройки биометрии тоже защищены

✅ **Настройка в профиле**
- Установка PIN с подтверждением
- Включение/отключение PIN защиты
- Включение/отключение биометрии
- Проверка доступности биометрии на устройстве

## Файловая структура

### Новые файлы:

```
lib/
├── services/
│   └── pin_security_service.dart       # Сервис для PIN защиты и биометрии
│
└── screens/
    └── auth/
        └── pin_entry_screen.dart        # Экран ввода PIN при запуске
```

### Обновленные файлы:

```
lib/
├── main.dart                        # Интеграция PIN защиты в поток приложения
├── providers/
│   └── app_settings_provider.dart   # Интеграция с PinSecurityService
│
└── screens/
    └── profile/
        ├── pin_setup_screen.dart     # Новый UI с цифровой клавиатурой
        └── profile_screen.dart       # Управление биометрией
```

## Зависимости

Добавлены в `pubspec.yaml`:

```yaml
dependencies:
  local_auth: ^2.1.8              # Биометрическая аутентификация
  flutter_secure_storage: ^9.0.0  # Безопасное хранение
```

## Установка

### 1. Установите зависимости:

```bash
flutter pub get
```

### 2. Настройте разрешения:

#### iOS (Info.plist):

Добавьте в `ios/Runner/Info.plist`:

```xml
<key>NSFaceIDUsageDescription</key>
<string>Используется для защиты ваших данных и быстрого входа в приложение</string>
```

#### Android (MainActivity.kt):

Добавьте в `android/app/src/main/kotlin/.../MainActivity.kt`:

```kotlin
import io.flutter.embedding.android.FlutterFragmentActivity

class MainActivity: FlutterFragmentActivity() {
    // Биометрия будет работать автоматически
}
```

### 3. Запустите приложение:

```bash
flutter run
```

## Как использовать

### Установка PIN:

1. Откройте приложение
2. Перейдите в **Профиль** (последняя вкладка)
3. В разделе **"🔒 Безопасность"** включите **"Защита PIN-кодом"**
4. Введите 4-значный PIN
5. Подтвердите PIN
6. (Опционально) Включите биометрию

### Включение биометрии:

1. Сначала установите PIN (см. выше)
2. В разделе **"Безопасность"** появится переключатель **"Face ID / Touch ID"**
3. Включите его
4. При следующем запуске приложение автоматически предложит биометрию

### Отключение PIN:

1. Перейдите в **Профиль**
2. Отключите **"Защита PIN-кодом"**
3. PIN и биометрия будут удалены

## Поток работы

```
Splash Screen
    ↓
LoadSettings (AppSettingsProvider)
    ↓
PIN включен?
    │
    ├── Да → PinEntryScreen
    │           │
    │           ├── Биометрия включена? → Автоматический запуск Face ID/Touch ID
    │           │
    │           └── Ввод PIN или биометрия
    │                   │
    │                   ├── Успех → RootShell (главное приложение)
    │                   │
    │                   └── 3 ошибки → Блокировка 30 секунд
    │
    └── Нет → RootShell (напрямую)
```

## Безопасность

### Защищенное хранение

- **iOS**: Keychain Services
- **Android**: EncryptedSharedPreferences (AES256)
- **Linux/Windows**: libsecret / Windows Credential Manager

### Меры безопасности

✅ PIN никогда не хранится в открытом виде  
✅ Используется аппаратное шифрование  
✅ Ограничение попыток ввода  
✅ Биометрические данные не хранятся в приложении  
✅ Защита от brute-force атак  

## Тестирование

### Проверьте следующие сценарии:

1. ✅ Установка PIN в первый раз
2. ✅ Ввод правильного PIN
3. ✅ Ввод неправильного PIN (3 раза)
4. ✅ Блокировка после 3 ошибок
5. ✅ Аутентификация через Face ID / Touch ID
6. ✅ Отключение PIN
7. ✅ Перезапуск приложения с PIN
8. ✅ Работа на iOS и Android

## Что дальше?

Следующие задачи:

1. ☐ **Видео инструкции для упражнений**
2. ☐ **Полный тёмный режим**

---

**🎉 PIN защита успешно реализована!**
